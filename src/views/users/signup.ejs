<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Flanes | SIGNUP</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/sidebar.css">
    <link rel="stylesheet" href="/css/movies.css">
    <link rel="stylesheet" href="/css/login-signup.css">
</head>
<body>
    <%- include('../partials/header.ejs')%>
    <main>
        <%- include('../partials/sidebar.ejs')%>
        <div class="title">
            <p>Crea tu cuenta</p>
        </div>
        <div class="form">
            <img src="/img/ticket.png" alt="Flan sonriendo" id="ticket">
            <form action="/users/login" method="POST">
                <div class="form-group">
                    <label for="name">Nombre:</label>
                    <input type="text" name="name" id="formName" class=<%= locals.errors && errors.name ? 'input-error' : null  %> value="<%= locals.oldData ? oldData.name : null %>">
                    <% if (locals.errors && errors.name) { %>
                        <div class="text-error">
                            <%= errors.name.msg %>
                        </div>
                    <% } %>
                </div>
                <div class="form-group">
                    <label for="lastName">Apellido:</label>
                    <input type="text" name="lastName" id="formLastName" class=<%= locals.errors && errors.lastName ? 'input-error' : null  %> value="<%= locals.oldData ? oldData.lastName : null %>">
                    <% if (locals.errors && errors.lastName) { %>
                        <div class="text-error">
                            <%= errors.lastName.msg %>
                        </div>
                    <% } %>
                </div>
                <div class="form-group">
                    <label for="email">Email:</label>
                    <input type="email" id="email" name="email" class=<%= locals.errors && errors.email ? 'input-error' : null  %> required>
                    <% if (locals.errors && errors.email) { %>
                        <div class="text-error">
                            <%= errors.email.msg %>
                        </div>
                    <% } %>
                </div>
                <div class="form-group">
                    <label for="password">Contraseña:</label>
                    <input type="password" id="password" name="password" class=<%= locals.errors && errors.password ? 'input-error' : null  %> required>
                    <% if (locals.errors && errors.password) { %>
                        <div class="text-error">
                            <%= errors.password.msg %>
                        </div>
                    <% } %>
                </div>
                <div class="form-group">
                    <label for="passwordRepeat">Repetir contraseña:</label>
                    <input type="password" id="passwordRepeat" name="passwordRepeat" class=<%= locals.errors && errors.passwordRepeat ? 'input-error' : null  %> value="<%= locals.oldData ? oldData.passwordRepeat : null %>">
                    <% if (locals.errors && errors.passwordRepeat) { %>
                        <div class="text-error">
                            <%= errors.passwordRepeat.msg %>
                        </div>
                    <% } %>
                </div>
                <button type="submit">Crear cuenta</button>
            </form>
            <p>Si ya tenés una cuenta, ingresa <a href="login">aquí</a>.</p>
        </div>
    </main>
    <%- include('../partials/footer.ejs')%>
    <script>
        function openNav() {
        document.getElementById("mySidebar").style.width = "300px";
    }

    function closeNav() {
        document.getElementById("mySidebar").style.width = "0";
    }
    </script>
    <script src="/js/sidebar.js"></script>
</body>
</html>
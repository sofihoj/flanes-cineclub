<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Flanes cineclub</title>
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    <link rel="stylesheet" href="/css/styles.css">
    <link rel="stylesheet" href="/css/sidebar.css">
    <link rel="stylesheet" href="/css/movies.css">
    <link rel="stylesheet" href="/css/login-signup.css">
</head>
<body>
    <%- include('../partials/header.ejs')%>
    <main>
        <%- include('../partials/sidebar.ejs')%>
        <div class="title">
            <p>Iniciar sesión</p>
        </div>
        <div class="form">
            <img src="/img/cute-flan.png" alt="Flan sonriendo">
            <form action="/users/login" method="POST">
                <div class="form-group">
                    <label for="email">Email:</label>
                    <input type="email" id="email" name="email" class=<%= locals.errors && errors.email ? 'input-error' : null  %> required>
                    <% if (locals.errors && errors.email) { %>
                        <div class="text-error">
                            <%= errors.email.msg %>
                        </div>
                    <% } %>
                </div>
                <div class="form-group">
                    <label for="password">Contraseña:</label>
                    <input type="password" id="password" name="password" class=<%= locals.errors && errors.password ? 'input-error' : null  %> required>
                    <% if (locals.errors && errors.password) { %>
                        <div class="text-error">
                            <%= errors.password.msg %>
                        </div>
                    <% } %>
                </div>
                <div class="form-group" id="rememberUser">
                    <input type="checkbox" id="remember_user" name="remember_user" />
                    <label for="remember_user">Recordar usuario</label>
                </div>
                <button type="submit">Iniciar sesión</button>
            </form>
            <p>Si no tienes una cuenta, crea una <a href="signup">aquí</a>.</p>
        </div>
    </main>
    <%- include('../partials/footer.ejs')%>
    <script>
        function openNav() {
        document.getElementById("mySidebar").style.width = "300px";
    }

    function closeNav() {
        document.getElementById("mySidebar").style.width = "0";
    }
    </script>
    <script src="/js/sidebar.js"></script>
</body>
</html>